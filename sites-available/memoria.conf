# =============================================================================
# Franco Memória - Sistema de memória histórica
# =============================================================================
# Server: memoria.francodarocha.sp.gov.br
# Service: memoria:80
# Frontend: /home/usr001/apps/francomemoria/site/bundle
# =============================================================================

# Redirect HTTP to HTTPS
server {
    listen 80;
    server_name memoria.francodarocha.sp.gov.br;
    return 301 https://$host$request_uri;
}

server {
    include conf.d/ssl.conf;
    server_name memoria.francodarocha.sp.gov.br;

    root /home/usr001/apps/francomemoria/site/bundle;
    index index.html index.htm;

    include conf.d/error-pages.conf;

    location / {
        try_files $uri $uri/ /index.html;
    }

    location /api/ {
        set $upstream $upstream_memoria;
        proxy_pass http://$upstream/;
        proxy_redirect off;
        include conf.d/proxy-params.conf;
    }
}
