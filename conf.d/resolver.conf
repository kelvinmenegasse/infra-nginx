# =============================================================================
# DNS Resolver - Docker internal DNS
# =============================================================================
# Permite que o Nginx inicie mesmo se alguns upstreams não estiverem disponíveis
# Inclua este arquivo no bloco http {}
# =============================================================================

resolver 127.0.0.11 valid=30s ipv6=off;
resolver_timeout 5s;
